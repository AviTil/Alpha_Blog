<h3 style="text-transform: capitalize;"> Welcome to <%= @user.username %>'s page</h3>
<p id="notice"><%= notice %></p>

<%= gravatar_for @user %>

<p style="padding-top: 10px;">
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p></br>

<h4 style="border-bottom: 1px solid; text-transform: capitalize;"><%= @user.username %>'s articles</h4></br>

<div align="center">
<%= will_paginate @user_articles %>
</div>

<%= render 'userarticles' %>

<div align="center">
<%= will_paginate @user_articles %>
</div>

<div class="article-actions">
<% if logged_in? && current_user==@user %>
<%= link_to 'Edit User', edit_user_path(@user), class: "btn btn-xs btn-success" %> |
<% end %>
<%= link_to 'Back to Users', users_path, class: "btn btn-xs btn-danger" %>
</div>